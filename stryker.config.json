{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_comment": "Mutation testing, run by hand rather than in CI. Coverage says which lines ran; this says whether anything would notice if they were wrong. Scoped to the two pure layers that everything else is built on: the serializers, hand-written and tested mostly by example, and the model, where a surviving mutant means a rule nothing checks. Run with `npm run mutation`.",
  "packageManager": "npm",
  "testRunner": "vitest",
  "vitest": {
    "configFile": "vitest.mutation.config.ts"
  },
  "mutate": [
    "src/serialization/*.ts",
    "!src/serialization/*.test.ts",
    "src/ontologymodel/*.ts",
    "!src/ontologymodel/*.test.ts"
  ],
  "reporters": ["json", "progress"],
  "htmlReporter": {
    "fileName": "mutation/index.html"
  },
  "coverageAnalysis": "perTest",
  "timeoutMS": 20000,
  "concurrency": 4,
  "jsonReporter": {
    "fileName": "mutation/report.json"
  }
}
